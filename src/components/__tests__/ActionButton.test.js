import React from 'react';
import { ActionButton } from '../ActionButton';

describe('`ActionButton`', () => {
  // should show icon based on `icon`
  // should call onClick prop
  // should have className based on `icon`
  it('should match snapshot');
});
