import React from 'react';
import { ProjectTabs } from '../ProjectTabs';

describe('`ProjectTabs`', () => {
  // state.currentTab
  // Tabs value should be currentTab
  // changeTab
  // Tabs onChange should be changeTab
  // changeTab should update currentTab
  it('should match snapshot');
});
