import React from 'react';
import Header from '../Header';

describe('`Header`', () => {
  // should call `onMenuClick` on menu click
  // when `mobileOpen` is on render a `SwipeableDrawer`
  it('should match snapshot');
});
